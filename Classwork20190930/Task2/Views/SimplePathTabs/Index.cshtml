@model IEnumerable<Task2.SimplePathTab>

@{Layout = null;}

<!DOCTYPE html>

<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Index</title>
    </head>
    <body>

        @foreach (var item in Model)
        {
            <img src='@Url.Content(item.Imgpath)' />
        }
    </body>
</html>